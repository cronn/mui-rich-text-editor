import { Typography } from "@mui/material";
import type { ReactElement } from "react";

export function RichTextEditor(): ReactElement {
  return <Typography>I am still a work in progress</Typography>;
}
